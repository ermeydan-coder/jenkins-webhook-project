pipeline {
    agent any
    stages {
        stage('build') {
            steps {
                echo 'All-Matthias'
                sh 'echo Integrating Jenkins Pipeline with GitHub Webhook using Jenkinsfile All'
            }
        }
    }
}
